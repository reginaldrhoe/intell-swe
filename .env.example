# =============================================================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# 
# IMPORTANT: This is a TEMPLATE file. Do NOT edit this file directly!
# 
# To set up your environment:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Never commit .env to git (it's in .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# LLM API KEYS (Required - Choose ONE)
# -----------------------------------------------------------------------------
# Get your OpenAI key from: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_KEY_HERE

# OR use Anthropic Claude (alternative to OpenAI)
# Get your Claude key from: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-YOUR_ANTHROPIC_KEY_HERE

# -----------------------------------------------------------------------------
# LLM Provider Configuration (Optional)
# -----------------------------------------------------------------------------
# Auto-detection priority: CREWAI_PROVIDER > ANTHROPIC_API_KEY > OPENAI_API_KEY
# Uncomment to explicitly set provider:
# CREWAI_PROVIDER=openai
# CREWAI_PROVIDER=anthropic

# Model selection (defaults shown):
# CREWAI_MODEL=gpt-4o-mini                    # For OpenAI
# CREWAI_MODEL=claude-3-5-haiku-20241022      # For Claude
# OPENAI_DEFAULT_TEMPERATURE=0.2              # Temperature for LLM responses

# CrewAI Configuration (Optional - only if using CrewAI agents)
# CrewAI enables multi-agent orchestration and task delegation
# Leave empty to auto-detect provider from API keys
# CREWAI_API_KEY=                             # If using CrewAI cloud (optional)
# See docs/LLM_SETUP_GUIDE.md for detailed CrewAI setup instructions

# -----------------------------------------------------------------------------
# Vector Database (Required)
# -----------------------------------------------------------------------------
# Qdrant URL - use service name when running in Docker Compose
QDRANT_URL=http://qdrant:6333

# Default collection name for RAG documents
RAG_COLLECTION=rag-poc

# -----------------------------------------------------------------------------
# Redis Configuration (Required)
# -----------------------------------------------------------------------------
# Redis for SSE (Server-Sent Events), caching, and Celery task queue
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# Database Configuration (Required - Choose ONE)
# -----------------------------------------------------------------------------
# The system supports both MySQL and PostgreSQL

# OPTION A: MySQL 8.0 (Current default in docker-compose.yml)
DATABASE_URL=mysql+pymysql://raguser:strongpassword@mysql:3306/rag_poc
MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=rag_poc
MYSQL_USER=raguser
MYSQL_PASSWORD=strongpassword

# OPTION B: PostgreSQL (Uncomment to use PostgreSQL instead)
# DATABASE_URL=postgresql://postgres:yourpassword@postgres:5432/intell_swe
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=yourpassword
# POSTGRES_DB=intell_swe

# Note: Uncomment the database service you want in docker-compose.yml
# For MySQL: services -> mysql
# For PostgreSQL: services -> postgres

# -----------------------------------------------------------------------------
# Application Configuration (Optional)
# -----------------------------------------------------------------------------
# Uncomment and customize as needed:
# APP_ENV=development
# LOG_LEVEL=INFO
# DEBUG=false

# -----------------------------------------------------------------------------
# Security Notes
# -----------------------------------------------------------------------------
# ⚠️  NEVER commit .env to git
# ⚠️  Change default passwords before production deployment
# ⚠️  Rotate API keys regularly
# ⚠️  Use different credentials for dev/staging/prod environments
